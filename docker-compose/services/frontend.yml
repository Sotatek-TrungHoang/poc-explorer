version: '3.9'

services:
  frontend:
    image: mobichain-frontend:local
    build:
      context: ../../frontend
      dockerfile: ./Dockerfile
    pull_policy: never
    platform: linux/amd64
    restart: always
    container_name: 'frontend'
    env_file:
      -  ../envs/common-frontend.env
